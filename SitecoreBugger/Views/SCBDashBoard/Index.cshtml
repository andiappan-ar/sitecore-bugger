@using SitecoreBugger.Site.Model.Global;
@model MasterData


@{
    ViewBag.Title = "DashBoard";
    Layout = "~/Views/SCBLayout/_Account.cshtml";
}

<link href="~/Content/SCB/Styles/sc-bugger-dashboard.css" rel="stylesheet" />

<div class="wrapper">
    <!--Left side nav bar-->
    @{Html.RenderPartial("P_SideNavBar");}

    <!-- Filter section and results  -->
    @{Html.RenderPartial("P_FilterAndResults");}

    <!--Error detail modal-->
    <div class="sc_bugger-element-modal sc_bugger-element modal error-detail-modal buger-element rounded-0" tabindex="-1" role="dialog">
        <div class="modal-dialog" role="document">
            <div class="modal-content rounded-0">
                <div class="modal-header sc_bugger-element-background-color rounded-0">
                    <h4 class="modal-title">Error details</h4>
                    <button type="button" class="close rounded-0" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body sc_bugger-element-background-color-light rounded-0">
                    <form ID="sc_bugger-form-mark-error" class="sc_bugger-form-validator" is-valid-form="false" novalidate>
                        <!--Error Id-->
                        <input type="hidden" id="errorID" />
                        <!--Error title-->
                        <div class="form-group">
                            <label for="errorTitle">Error title</label>
                            <input type="text" class="form-control" id="errorTitle" placeholder="Error title" required>
                            <div class="valid-feedback">
                                Looks good!
                            </div>
                            <div class="invalid-feedback">Please fill out this field.</div>
                        </div>
                        <!--Error detail-->
                        <div class="form-group">
                            <label class="" for="errorDetail">Error description (Html allowed)</label>
                            <textarea class="form-control" id="errorDetail" rows="3" required></textarea>
                            <div class="valid-feedback">
                                Looks good!
                            </div>
                            <div class="invalid-feedback">Please fill out this field.</div>
                        </div>
                        <!--Error type-->
                        <div class="form-group">
                            <label class="" for="exampleFormControlSelect1">Error type</label>
                            <select class="form-control rounded-0" id="errorType" required>
                                @foreach (ValuePair val in Model.ErrorType)
                                {
                                    <option value="@val.EId">@val.EDisplayName</option>
                                }
                            </select>
                            <div class="valid-feedback">
                                Looks good!
                            </div>
                            <div class="invalid-feedback">Please fill out this field.</div>
                        </div>
                        <!--Error Severity-->
                        <div class="form-group">
                            <label class="" for="errorSeverity">Error Severity</label>
                            <select class="form-control rounded-0" id="errorSeverity" required>
                                @foreach (ValuePair val in Model.ErrorSeverity)
                                {
                                    <option value="@val.EId">@val.EDisplayName</option>
                                }
                            </select>
                            <div class="valid-feedback">
                                Looks good!
                            </div>
                            <div class="invalid-feedback">Please fill out this field.</div>
                        </div>
                        <!--Error Status-->
                        <div class="form-group">
                            <label class="" for="errorSeverity">Error Status</label>
                            <select class="form-control rounded-0" id="errorStatus" required>
                                @foreach (ValuePair val in Model.ErrorStatus)
                                {
                                    <option value="@val.EId">@val.EDisplayName</option>
                                }
                            </select>
                            <div class="valid-feedback">
                                Looks good!
                            </div>
                            <div class="invalid-feedback">Please fill out this field.</div>
                        </div>

                        <!--Assighned user-->
                        <div class="form-group">
                            <label class="" for="filter-error-status">Assighned to:</label>
                            <select class="form-control" id="filter-a-user-id" required>
                                @foreach (User val in Model.UserList)
                                {
                                    <option value="@val.UserId" r-id="@val.RoleId">@val.UserName</option>
                                }
                            </select>
                            <div class="valid-feedback">
                                Looks good!
                            </div>
                            <div class="invalid-feedback">Please fill out this field.</div>
                        </div>

                        <!--Owner user-->
                        <div class="form-group">
                            <label class="" for="filter-o-user-id">Owner:</label>
                            <select class="form-control" id="filter-o-user-id" disabled>
                                <option value="">Select</option>
                                @foreach (User val in Model.UserList)
                                {
                                    <option value="@val.UserId" r-id="@val.RoleId">@val.UserName</option>
                                }
                            </select>
                            <div class="valid-feedback">
                                Looks good!
                            </div>
                            <div class="invalid-feedback">Please fill out this field.</div>
                        </div>


                        <!--Device detail-->
                        <div class="form-group">
                            <p>
                                <a class="btn btn-primary rounded-0" data-toggle="collapse" href="#collapseExample" role="button"
                                   aria-expanded="false" aria-controls="collapseExample">
                                    Device details -
                                    <i class="fa fa-arrows-v" aria-hidden="true"></i>
                                </a>
                            </p>
                            <div class="collapse" id="collapseExample">
                                <div id="browser-detail" class="card card-body">
                                </div>
                            </div>
                        </div>
                        <!--screen shot-->
                        <div class="form-group sc_bugger-screen-shot-div">
                            <div class="custom-file">
                                <input id="sc_bugger-upload-screenshot" accept="image/*" type="file" class="custom-file-input" is-updated-creenshot="false">
                                <label class="custom-file-label" for="validatedCustomFile">Upload screen shot...</label>
                                <div class="invalid-feedback">Example invalid custom file feedback</div>
                            </div>
                            <figure class="figure">
                                <br />
                                <img id="error-screen-shot" class="figure-img img-fluid rounded">
                                <figcaption class="figure-caption text-right">Screen shot of an issue.</figcaption>
                            </figure>
                        </div>
                        <button id="hidden-submit" class="sc_bugger-hide rounded-0" type="submit">Submit form</button>
                    </form>
                </div>
                <div class="modal-footer sc_bugger-element-background-color rounded-0">
                    <button id="sc_bugger-submit-error" type="button" class="btn btn-primary rounded-0">Save</button>
                    <button type="button" class="btn btn-secondary rounded-0" data-dismiss="modal">Close</button>
                </div>
            </div>
        </div>
    </div>

</div>

<div class="sc_bugger-element">
    <!--Loader spinning wheel-->
    <div class="modal hide text-white " id="pleaseWaitDialog" data-backdrop="static" data-keyboard="false" data-html2canvas-ignore="true">
        <div class="modal-header" data-html2canvas-ignore="true">
            <h1 data-html2canvas-ignore="true">Please Wait</h1>
        </div>
        <div class="modal-body" data-html2canvas-ignore="true">
            <div class="spinner-border" style="width: 5rem; height: 5rem;" role="status" data-html2canvas-ignore="true">
            </div>
        </div>
    </div>
</div>

<script src="~/Scripts/SCB/sc-bugger-dashboard.js"></script>


